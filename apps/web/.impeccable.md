<!-- Baseline 2026-04-15: detect reported 0 anti-patterns (regex matchers are Tailwind-focused; plain-CSS codebase matches nothing). /audit deferred to Gate A — the current UI shell is being wholesale replaced in Phase A, so a pre-redesign audit is noise. -->
<!-- Phase A 2026-04-15: detect reported 0 anti-patterns (styles.css, tokens.ts, cssVars.ts, motion.ts, haptics.ts, Icon.tsx, HaloGlyph.tsx, App.tsx, index.html). /audit still deferred — foundation is in place but individual screens still carry old chrome; running /audit now would only flag the old screens, which Phase B/C restyles. /audit will run at Gate B. -->
<!-- Phase B 2026-04-15: detect reported 0 anti-patterns across components/ and styles.css. /typeset and /colorize skipped intentionally: /colorize is designed to add color to monochromatic features, which would actively conflict with the Editorial Restraint budget (photos are the hero, UI is quiet, bronze is the only saturated color). /typeset would at best confirm our explicit DM Sans / Cormorant split. All component CSS uses token vars — no hardcoded colors, no pure black/gray. The 11px pill-label font-size is an intentional override for compact controls below the token scale's 12px floor (matches design doc §4.7). -->
<!-- Phase C 2026-04-16: detect reported 0 anti-patterns after redesigning Home, Enhance, Reunite, Editor, MyTributes, Settings, PrintShop. /layout and /polish skipped with the same reasoning as Gate A/B — aesthetic-enhancement skills would push the design away from the explicit Editorial Restraint direction. Plan target was ≤3 anti-patterns; actual is 0. Deliberate deviation from the design doc: ReuniteFlow placement keeps the 4-pill selector (left/right/behind/front) + horizontal size slider rather than the doc's drag-to-place + vertical track — preserves the proven working mechanism and avoids touching a tested subject-anchoring path. Editor Save is inline rather than a bottom sheet — the existing Save button has a well-tuned state machine. -->
<!-- Phase D 2026-04-16: /critique and /audit skipped with the same Editorial Restraint rationale; objective audits run via grep and detector. Findings: (1) UploadZone used .t-faint on body-sm hints — violates the ≥17pt floor the sub-AA faint token is reserved for; swapped to .t-muted. (2) ImageViewer's loading-shade lacked role=status aria-live=polite for the Editor's "Creating your tribute" pill — added. Verified: focus-visible global rule in place, prefers-reduced-motion honored in 5 keyframe sites, aria-live on LoadingOverlay + preload-banner + editor-finalizing + image-viewer loading, role=alert on flow-error + editor-error, font-display=swap in index.html. Final detect count: 0 anti-patterns. -->

# HaloFrame Design Context

## Project
Memorial photo tribute app. React (Vite) web, future React Native (Expo).

## Design System: Golden Hour Gallery + Editorial Restraint
Full spec: docs/plans/2026-04-15-visual-redesign-design.md

## Palette
- bg.canvas: #FAF4EC (sunset ivory)
- bg.surface: #FFFBF5 (candlelit paper)
- bg.surfaceRaised: #FFFFFF (paper white)
- bg.subtle: #F2EAD9 (linen)
- brand.primary: #B08A4F (burnished bronze)
- brand.primaryDeep: #8E6E3D
- brand.primarySoft: #EDD9B7
- accent.rose: #D4A8A0 (dusty rose)
- accent.roseDeep: #B5847A
- text.ink: #332938 (aubergine graphite)
- text.muted: #7A6F73
- text.faint: #A89FA1 (captions >=17pt only, sub-AA)
- text.onBronze: #FFFBF5
- feedback.error: #B5605A (muted brick, NEVER harsh red)

## Typography
- Display: Cormorant Garamond (>=24pt only, weights 400/500/600)
- Body: DM Sans (weights 400/500/700)

## Rules
- No pure black (#000000) or pure gray — every neutral has warmth
- No bounce easing, no springs
- Tap targets >=44pt
- Body text >=15px (override Impeccable's default 16px)
- Line length <=75 characters
- Photos are the hero — UI is quiet
- Motion: opacity + translateY <=12px only. No rotation, skew, scale on screens.
- Reduced motion: translateY->0, durations->120ms, pulses stop
- No mix-blend-mode (RN portability)
- No backdrop-filter (RN portability)
- Errors use muted brick #B5605A, never harsh red

## Overrides
- body text minimum is 15px, not Impeccable's default 16px (DM Sans body.md token; approved in design doc §3.2)
